# Migration generation

`autosql migrate generate` verifies and locks the migration snapshot, plans an
explicit current source against an explicit desired source, and publishes the
new SQL generation with a manifest compare-and-swap. Version, label, format,
and rename hints are explicit and bound into the generated bytes. A semantic
no-op does not write the directory. Concurrent generators using the same head
cannot both publish.
